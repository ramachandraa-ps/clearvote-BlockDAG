specVersion: "v1"
serviceId: "clearvote"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "clearvote-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
